You are a finance data compiler. Using the Step 1 anchors and context, emit a retrieval plan JSON for Step 2.

Original question:
{{question}}

Step 1 payload:
{{step1_json}}

Respond with JSON following the step2 schema (batches with requests). If you can already answer, you may include a brief natural-language answer instead.
